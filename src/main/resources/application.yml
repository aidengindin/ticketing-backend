spring:
    # datasource:
    #     url: jdbc:postgresql://localhost:5432/ticketing
    #     username: ticketing
    #     password: ticketing
    jpa:
        hibernate:
            ddl-auto: none
        open-in-view: false
    sql:
        init:
            mode: always
